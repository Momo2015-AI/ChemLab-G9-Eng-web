export function renderAssessmentPortal({root,onHome=()=>{window.location.hash='home'},score=0,weakPoints=[],tasks=[] ,onTask=()=>{}}={}){if(!root)return;root.innerHTML=`<section class="portal-page"><div class="portal-hero"><div><div class="portal-eyebrow">ASSESSMENT CENTER</div><h1 class="portal-title">训练与诊断</h1><p class="portal-subtitle">答题结果不只是分数，而是下一轮学习的证据。</p></div><button class="portal-btn" data-home>⌂ 首页</button></div><div class="portal-grid"><article class="portal-card"><div class="portal-muted">最近成绩</div><div class="portal-value">${score || '--'}${score ? '%' : ''}</div><span class="portal-chip good">Evidence</span></article><article class="portal-card wide"><h3>知识诊断</h3><p class="portal-muted">薄弱知识点会进入补救学习和 targeted recheck。</p><div>${weakPoints.length?weakPoints.map(x=>`<span class="portal-chip">${escapeHtml(x.name||x.id||x)}</span>`).join(''):'<span class="portal-chip good">暂无明显薄弱点</span>'}</div></article><article class="portal-card full"><h3>待完成任务</h3>${tasks.length?`<div class="portal-list assessment-task-list">${tasks.map(task=>`<button type="button" class="assessment-task" data-task="${escapeHtml(task.id)}" ${task.available===false?'disabled':''}><strong>${escapeHtml(task.title)}</strong><span>${escapeHtml(task.description||'')}</span><small>${escapeHtml(task.label||'')}</small></button>`).join('')}</div>`:'<p class="portal-muted">当前没有待完成任务，完成课程学习后这里会自动更新。</p>'}</article></div></section>`;root.querySelector('[data-home]')?.addEventListener('click',onHome);root.querySelectorAll('[data-task]').forEach(button=>button.addEventListener('click',()=>onTask(tasks.find(task=>task.id===button.dataset.task))));}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
