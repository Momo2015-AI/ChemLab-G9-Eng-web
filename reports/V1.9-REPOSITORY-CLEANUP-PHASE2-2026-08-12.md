# V1.9 Repository Cleanup — Phase 2 Audit

Date: 2026-08-12
Branch: main

## Scope

Deep audit of canonical content ownership, duplicate schemas, obsolete V1.6 content helpers, runtime boundaries, CI references, and recent cleanup history.

## Findings

### Confirmed obsolete

- `content/course-schema.js` — explicitly labeled V1.6 and no longer part of the canonical content contract.
- `schemas/lesson.schema.json` — duplicate legacy lesson contract superseded by `content/schema/lesson.schema.json`.
- `schemas/question.schema.json` — duplicate legacy question contract superseded by `content/schema/question.schema.json`.

### Retained intentionally

- `schemas/experiment.schema.json` — no duplicate content/schema counterpart was established during this pass.
- `schemas/instrument.schema.json` — distinct global/instrument contract; not treated as an educational-content duplicate.
- `content/schema/*` — canonical machine-readable educational content contracts.
- `modules/questions/question-bank.json` and taxonomy compatibility data — retained because current integrity tooling/runtime still consumes them; migration is a separate controlled task, not a cleanup deletion.
- `engine/*` — retained because production bootstrap imports assessment and experiment engines.

## Actions

Deleted:

1. `content/course-schema.js`
2. `schemas/lesson.schema.json`
3. `schemas/question.schema.json`

Added:

- `docs/REPOSITORY-CANONICAL-MAP.md`
- this audit report

## Verification requirements

The cleanup must pass:

- JavaScript syntax validation
- test suite
- runtime architecture audit
- JSON validation
- content integrity gate
- deployment entry check

No content is marked ready merely to make CI green.

## Next action

Run the post-cleanup CI. If it passes structurally but remains blocked by lesson content readiness, address content quality rather than weakening the integrity gate. Continue the canonical-source migration only where a concrete duplicate or orphan is proven.
