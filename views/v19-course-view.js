/** V2.8 Course View — continuous lesson context for learning routes. */
export function renderV19Course({ root, lesson = {}, guidedLearning = null, progress = false, masteryPassed = false, diagnosis = {}, onStartQuiz, onStartMastery, onStartExperiment, onStartRemediation, onComplete, onBack } = {}) {
  if (!root) return;
  const lessonId = lesson.id || lesson.canonicalId || '';
  if (typeof window !== 'undefined' && lessonId) window.__chemLabCurrentLessonId = lessonId;
  const points = Array.isArray(lesson.knowledgePoints) ? lesson.knowledgePoints : [];
  const experiments = Array.isArray(lesson.experiments) ? lesson.experiments : [];
  const steps = Array.isArray(guidedLearning?.steps) ? guidedLearning.steps : [];
  const mastery = lesson.mastery || {};
  const masteryCount = Number(mastery.questionCount || 20);
  const masteryThreshold = Math.round(Number(mastery.threshold || .95) * 100);
  const passed = Boolean(masteryPassed);
  const errors = Array.isArray(diagnosis?.errors) ? diagnosis.errors : [];
  const weakPoints = Array.isArray(diagnosis?.weakPoints) ? diagnosis.weakPoints : [];
  root.innerHTML = `<section class="page course-page v21-course">
    <div class="course-hero-card"><div class="course-hero-top"><button class="course-back" type="button" data-back>← 学习中心</button><span class="course-status ${passed || progress ? 'done' : ''}">${passed || progress ? '✓ 已掌握' : '● 学习中'}</span></div><div class="course-hero-main"><div><div class="course-kicker">第 ${escapeHtml(lesson.day || lesson.sequenceNumber || '')} 课 · ${escapeHtml(lesson.duration || '')}</div><h1>${escapeHtml(lesson.title || lesson.id || '课程')}</h1><p>${escapeHtml(lesson.summary || lesson.description || '')}</p></div><div class="course-hero-icon" aria-hidden="true">⚗</div></div><div class="course-flow" aria-label="完整学习流程"><span class="active">① 学习</span><i>→</i><span>② 实验</span><i>→</i><span>③ 练习</span><i>→</i><span>④ 诊断/补救</span><i>→</i><span class="${passed ? 'passed' : ''}">⑤ ${masteryThreshold}%掌握</span></div></div>
    <section class="course-section-block lesson-goal-block"><div class="course-section-heading"><div><span class="section-eyebrow">学习目标</span><h2>本节课要掌握什么？</h2></div><span class="section-count">${points.length} 个知识点</span></div><div class="knowledge-card-grid">${points.length ? points.map((id, i) => `<article class="knowledge-card-v21"><span class="knowledge-index">${String(i + 1).padStart(2,'0')}</span><div><strong>${escapeHtml(formatKnowledgePoint(id))}</strong><small>本课重点</small></div></article>`).join('') : '<div class="empty-card">本课暂无独立知识点配置。</div>'}</div></section>
    ${steps.length ? `<section class="course-section-block lesson-content-block" id="guided-learning"><div class="course-section-heading"><div><span class="section-eyebrow">第一阶段 · 核心学习</span><h2>一步一步学</h2><p class="section-description">先理解，再检查；每一步都包含知识讲解与即时检查。</p></div><span class="section-count">${steps.length} 步</span></div><div class="guided-learning-cards">${steps.map((step, i) => renderGuidedCard(step, i)).join('')}</div></section>` : ''}
    ${experiments.length ? `<section class="course-section-block" id="experiment-section"><div class="course-section-heading"><div><span class="section-eyebrow">第二阶段 · 实验探究</span><h2>观察 → 证据 → 结论</h2><p class="section-description">把刚学的概念放进实验情境中，训练科学判断。</p></div><span class="section-count">${experiments.length} 个实验</span></div><div class="course-resource-card"><div><strong>${escapeHtml(experiments[0].title || '实验探究')}</strong><p>${escapeHtml(experiments[0].goal || experiments[0].purpose || '观察现象、记录证据并形成结论。')}</p></div><button class="course-action primary inline-action" type="button" data-experiment>进入实验 →</button></div></section>` : ''}
    <section class="course-section-block" id="practice-section"><div class="course-section-heading"><div><span class="section-eyebrow">第三阶段 · 基础练习</span><h2>练习：检查是否真正理解</h2><p class="section-description">先完成基础题，再进入诊断与补救。</p></div><span class="section-count">${Array.isArray(lesson.questions) ? lesson.questions.length : 0} 题</span></div><div class="course-resource-card"><div><strong>基础练习</strong><p>覆盖本课核心概念、辨析和证据推理。</p></div><button class="course-action primary inline-action" type="button" data-quiz>开始练习 →</button></div></section>
    <section class="course-section-block" id="diagnostic-section"><div class="course-section-heading"><div><span class="section-eyebrow">第四阶段 · 诊断与补救</span><h2>诊断：找到不会的地方</h2><p class="section-description">这里直接列出本次练习的错误、知识点和补救方向。</p></div><span class="section-count">${errors.length} 个错误</span></div>${errors.length ? `<div class="diagnostic-error-list">${errors.map((error, i) => `<article class="diagnostic-error-card"><div class="diagnostic-error-index">${String(i + 1).padStart(2,'0')}</div><div><strong>第 ${escapeHtml(error.questionId || '未知题')}</strong><p>${escapeHtml(error.possibleErrors?.[0] || error.mistake || '需要重新检查这道题的判断依据。')}</p>${error.explanation ? `<small>${escapeHtml(error.explanation)}</small>` : ''}${Array.isArray(error.knowledgeIds) && error.knowledgeIds.length ? `<small>需要复习：${error.knowledgeIds.map(formatKnowledgePoint).map(escapeHtml).join('、')}</small>` : ''}</div></article>`).join('')}</div><div class="diagnostic-next"><strong>下一步：针对性补救 → 再检测</strong><p>${weakPoints.length ? `优先复习：${weakPoints.map(formatKnowledgePoint).map(escapeHtml).join('、')}` : '完成针对性补救后，再进行检测。'}</p><button class="course-action primary inline-action" type="button" data-remediation>进入补救 →</button></div>` : `<div class="diagnostic-empty"><strong>还没有形成错题记录</strong><p>完成基础练习后，如果有错题，这里会自动列出具体错误、知识点和补救建议。</p></div>`}</section>
    <section class="course-section-block" id="mastery-section"><div class="course-section-heading"><div><span class="section-eyebrow">第五阶段 · 最终掌握</span><h2>${masteryThreshold}% Mastery</h2><p class="section-description">用未直接见过的变式和陌生情境检验迁移能力。</p></div><span class="section-count">${masteryCount} 题 · ≥${masteryThreshold}%</span></div><div class="mastery-gate-card"><div><strong>${passed ? '✓ 已达到本课掌握标准' : '真正掌握的标准'}</strong><p>${escapeHtml(mastery.description || `${masteryCount}题至少答对${Math.ceil(masteryCount * masteryThreshold / 100)}题，才算本课掌握。`)}</p></div><button class="course-action primary inline-action" type="button" data-mastery>${passed ? '再次挑战 Mastery →' : '开始 Mastery →'}</button></div></section>
    ${!progress ? `<div class="course-completion-bar"><button class="course-action ${passed ? 'primary' : 'quiet'}" type="button" data-complete ${passed ? '' : 'disabled aria-disabled="true"'}><span class="action-icon">✓</span><span><strong>完成本课</strong><small>${passed ? 'Mastery ≥95%，现在可以完成本课' : '完成 Mastery 达标后自动解锁'}</small></span></button><div class="completion-hint">${passed ? '已满足完成条件：Mastery 达标。' : `当前流程：先学习 → 实验 → 练习 → 诊断/补救 → ${masteryThreshold}% Mastery`}</div></div>` : ''}
  </section>`;
  root.querySelector('[data-back]')?.addEventListener('click', () => onBack?.()); root.querySelector('[data-quiz]')?.addEventListener('click', () => onStartQuiz?.()); root.querySelector('[data-mastery]')?.addEventListener('click', () => onStartMastery?.()); root.querySelector('[data-experiment]')?.addEventListener('click', () => onStartExperiment?.(experiments[0]?.id || experiments[0])); root.querySelector('[data-complete]')?.addEventListener('click', () => { if (passed) onComplete?.(); }); root.querySelector('[data-remediation]')?.addEventListener('click', () => onStartRemediation?.());
}
const KNOWLEDGE_LABELS={'matter-change':'物质的变化','physical-change':'物理变化','chemical-change':'化学变化','physical-property':'物理性质','chemical-property':'化学性质','observation-inference':'观察与推理','evidence-reasoning':'证据与结论'};
function formatKnowledgePoint(id){return KNOWLEDGE_LABELS[id]||id;}
function renderGuidedCard(step,index){const body=Array.isArray(step.body)?step.body:[step.body];const check=step.check;const hasDetail=body.some(Boolean)||Boolean(check);const label=step.type==='concept'?'核心概念':step.type==='reasoning'?'判断方法':step.type==='transfer'?'迁移检查':step.type==='compare'?'综合辨析':'引导学习';return `<article class="guided-learning-card ${hasDetail?'':'compact'}"><button class="guided-card-header" type="button" aria-expanded="false"><span class="guided-card-index">${String(index+1).padStart(2,'0')}</span><span class="guided-card-main"><span class="guided-card-label">第 ${index+1} 步 · ${label}</span><strong>${escapeHtml(step.title||'')}</strong><small>点击展开完整讲解与检查</small></span><span class="guided-card-toggle" aria-hidden="true">＋</span></button><div class="guided-card-detail" hidden><div class="guided-step-body">${body.filter(Boolean).map(text=>`<p>${escapeHtml(text)}</p>`).join('')}</div>${check?renderCheck(check,index):''}</div></article>`;}
function renderCheck(check,index){const options=Array.isArray(check.options)?check.options:[];return `<div class="guided-check" data-answer="${Number.isInteger(check.answer)?check.answer:''}" data-explanation="${escapeHtml(check.explanation||'请回到本步骤的讲解，找出支持答案的证据。')}"><div class="guided-check-title">马上检查一下</div><p class="guided-question">${escapeHtml(check.question||'')}</p>${options.length?`<div class="guided-options">${options.map((option,i)=>`<label><input type="radio" name="guided-check-${index}" value="${i}"><span>${String.fromCharCode(65+i)}. ${escapeHtml(option)}</span></label>`).join('')}</div>`:''}<button type="button" class="guided-submit">提交答案</button><div class="guided-result" hidden></div></div>`;}
function escapeHtml(value){return String(value).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
if(typeof document!=='undefined')document.addEventListener('click',event=>{const header=event.target.closest('.guided-card-header');if(header){const card=header.closest('.guided-learning-card');const detail=card?.querySelector('.guided-card-detail');if(!detail)return;const open=!detail.hidden;detail.hidden=open;header.setAttribute('aria-expanded',String(!open));const toggle=header.querySelector('.guided-card-toggle');if(toggle)toggle.textContent=open?'＋':'−';return;}const submit=event.target.closest('.guided-submit');if(submit){const check=submit.closest('.guided-check');const selected=check?.querySelector('input[type="radio"]:checked');const result=check?.querySelector('.guided-result');if(!selected||!result)return;const expected=Number.parseInt(check.dataset.answer,10);const correct=Number.parseInt(selected.value,10)===expected;result.hidden=false;result.textContent=correct?`✓ 回答正确。${check.dataset.explanation}`:`再想一想。${check.dataset.explanation}`;result.className=`guided-result ${correct?'correct':'retry'}`;}});
