/** V2.8 Course View — continuous lesson context for learning routes. */
export function renderV19Course({ root, lesson = {}, guidedLearning = null, progress = false, masteryPassed = false, diagnosis = {}, diagnosticQuestions = [], lessonState = {}, phase = 'LEARNING', stages = {}, highlightStep = '', onGuidedCheck, onStartQuiz, onStartMastery, onStartExperiment, onStartRemediation, onStartTransfer, onComplete, onBack } = {}) {
  if (!root) return;
  const lessonId = lesson.id || lesson.canonicalId || '';
  if (typeof window !== 'undefined' && lessonId) window.__chemLabCurrentLessonId = lessonId;
  const points = Array.isArray(lesson.knowledgePoints) ? lesson.knowledgePoints : [];
  const experiments = Array.isArray(lesson.experiments) ? lesson.experiments : [];
  const steps = Array.isArray(guidedLearning?.steps) ? guidedLearning.steps : [];
  const guidedSteps = lessonState?.guided?.steps || {};
  const guidedDone = steps.filter(step => guidedSteps[step.id]?.correct).length;
  const mastery = lesson.mastery || {};
  const masteryCount = Number(mastery.questionCount || 20);
  const masteryThreshold = Math.round(Number(mastery.threshold || .95) * 100);
  const passed = Boolean(masteryPassed);
  const errors = Array.isArray(diagnosis?.errors) ? diagnosis.errors : [];
  const weakPoints = Array.isArray(diagnosis?.weakPoints) ? diagnosis.weakPoints : [];
  root.innerHTML = `<section class="page course-page v21-course">
    <div class="course-hero-card"><div class="course-hero-top"><button class="course-back" type="button" data-back>← 学习中心</button><span class="course-status ${passed || progress ? 'done' : ''}">${passed || progress ? '✓ 已掌握' : '● 学习中'}</span></div><div class="course-hero-main"><div><div class="course-kicker">第 ${escapeHtml(lesson.day || lesson.sequenceNumber || '')} 课 · ${escapeHtml(lesson.duration || '')}</div><h1>${escapeHtml(lesson.title || lesson.id || '课程')}</h1><p>${escapeHtml(lesson.summary || lesson.description || '')}</p></div><div class="course-hero-icon" aria-hidden="true">⚗</div></div><div class="course-flow" aria-label="完整学习流程"><span class="active">① 学习</span><i>→</i><span>② 实验</span><i>→</i><span>③ 练习</span><i>→</i><span>④ 诊断/补救</span><i>→</i><span class="${passed ? 'passed' : ''}">⑤ ${masteryThreshold}%掌握</span></div></div>
    <section class="course-section-block lesson-goal-block"><div class="course-section-heading"><div><span class="section-eyebrow">学习目标</span><h2>本节课要掌握什么？</h2></div><span class="section-count">${points.length} 个知识点</span></div><div class="knowledge-card-grid">${points.length ? points.map((id, i) => `<article class="knowledge-card-v21"><span class="knowledge-index">${String(i + 1).padStart(2,'0')}</span><div><strong>${escapeHtml(formatKnowledgePoint(id))}</strong><small>本课重点</small></div></article>`).join('') : '<div class="empty-card">本课暂无独立知识点配置。</div>'}</div></section>
    ${renderLessonSections(lesson.sections)}
    ${steps.length ? `<section class="course-section-block lesson-content-block" id="guided-learning"><div class="course-section-heading"><div><span class="section-eyebrow">第一阶段 · 核心学习</span><h2>一步一步学</h2><p class="section-description">先理解，再检查；每一步都包含知识讲解与即时检查。</p></div><span class="section-count" data-guided-counter>${guidedDone}/${steps.length} 步 · ${phaseLabel(phase)}</span></div><div class="guided-learning-cards">${steps.map((step, i) => renderGuidedCard(step, i, lessonId, guidedSteps)).join('')}</div></section>` : ''}
    ${experiments.length ? `<section class="course-section-block" id="experiment-section"><div class="course-section-heading"><div><span class="section-eyebrow">第二阶段 · 实验探究</span><h2>观察 → 证据 → 结论</h2><p class="section-description">把刚学的概念放进实验情境中，训练科学判断。</p></div><span class="section-count">${experiments.length} 个实验</span></div><div class="course-resource-card"><div><strong>${escapeHtml(experiments[0].title || '实验探究')}</strong><p>${escapeHtml(experiments[0].goal || experiments[0].purpose || '观察现象、记录证据并形成结论。')}</p></div><button class="course-action primary inline-action" type="button" data-experiment ${stages.experiment ? '' : 'disabled aria-disabled="true"'}>${stages.experiment ? '进入实验 →' : '完成学习后解锁'}</button></div></section>` : ''}
    <section class="course-section-block" id="practice-section"><div class="course-section-heading"><div><span class="section-eyebrow">第三阶段 · 基础练习</span><h2>练习：检查是否真正理解</h2><p class="section-description">先完成基础题，再进入诊断与补救。</p></div><span class="section-count">${Array.isArray(lesson.questions) ? lesson.questions.length : 0} 题</span></div><div class="course-resource-card"><div><strong>基础练习</strong><p>覆盖本课核心概念、辨析和证据推理。</p></div>${stages.prerequisitesMet === false ? `<button class="course-action primary inline-action" type="button" disabled aria-disabled="true">完成前置课程后解锁</button><small style="display:block;margin-top:6px;color:var(--ink-dim)">需先掌握：${(stages.prerequisitesMissing || []).map(formatKnowledgePoint).map(escapeHtml).join('、')}</small>` : stages.practice ? `<button class="course-action primary inline-action" type="button" data-quiz>开始练习 →</button>` : `<button class="course-action primary inline-action" type="button" disabled aria-disabled="true">完成实验后解锁</button><small style="display:block;margin-top:6px;color:var(--ink-dim)">请先完成引导学习和实验探究，再进行练习。</small>`}</div></section>
    <section class="course-section-block" id="diagnostic-section"><div class="course-section-heading"><div><span class="section-eyebrow">第四阶段 · 诊断与补救</span><h2>诊断：找到不会的地方</h2><p class="section-description">这里直接列出本次练习的错误、知识点和补救方向。</p></div><span class="section-count">${errors.length} 个错误</span></div>${errors.length ? `<div class="diagnostic-error-list">${errors.map((error, i) => `<article class="diagnostic-error-card"><div class="diagnostic-error-index">${String(i + 1).padStart(2,'0')}</div><div><strong>第 ${escapeHtml(error.questionId || '未知题')}</strong><p>${escapeHtml(error.possibleErrors?.[0] || error.mistake || '需要重新检查这道题的判断依据。')}</p>${error.explanation ? `<small>${escapeHtml(error.explanation)}</small>` : ''}${Array.isArray(error.knowledgeIds) && error.knowledgeIds.length ? `<small>需要复习：${error.knowledgeIds.map(formatKnowledgePoint).map(escapeHtml).join('、')}</small>` : ''}</div></article>`).join('')}</div><div class="diagnostic-next"><strong>下一步：针对性补救 → 再检测</strong><p>${weakPoints.length ? `优先复习：${weakPoints.map(formatKnowledgePoint).map(escapeHtml).join('、')}` : '完成针对性补救后，再进行检测。'}</p><button class="course-action primary inline-action" type="button" data-remediation>进入补救 →</button></div>` : `<div class="diagnostic-empty"><strong>还没有形成错题记录</strong><p>完成基础练习后，如果有错题，这里会自动列出具体错误、知识点和补救建议。</p></div>`}${diagnosticQuestions.length ? `<div class="diagnostic-preset"><div class="course-section-heading"><div><span class="section-eyebrow">预置诊断自查</span><h3>针对本课核心概念的自我检查</h3><p class="section-description">每一道自查题都指向具体的引导学习步骤；答不上来时，点击链接回到对应步骤重新学习。</p></div></div><div class="preset-question-list">${diagnosticQuestions.map((item, i) => `<article class="preset-question-card"><div class="preset-question-index">${String(i + 1).padStart(2,'0')}</div><div class="preset-question-body"><strong>${escapeHtml(item.question || '')}</strong>${item.remediationStep ? `<small>复习指引：${item.remediationStep.split('/').map(stepId => `<button type="button" class="link-btn" data-guided-step="${escapeHtml(stepId)}">${escapeHtml(stepId)}</button>`).join(' → ')}</small>` : ''}</div></article>`).join('')}</div></div>` : ''}</section>
    <section class="course-section-block" id="mastery-section"><div class="course-section-heading"><div><span class="section-eyebrow">第五阶段 · 最终掌握</span><h2>${masteryThreshold}% Mastery</h2><p class="section-description">用未直接见过的变式和陌生情境检验迁移能力。</p></div><span class="section-count">${masteryCount} 题 · ≥${masteryThreshold}%</span></div><div class="mastery-gate-card"><div><strong>${passed ? '✓ 已达到本课掌握标准' : '真正掌握的标准'}</strong><p>${escapeHtml(mastery.description || `${masteryCount}题至少答对${Math.ceil(masteryCount * masteryThreshold / 100)}题，才算本课掌握。`)}</p></div><button class="course-action primary inline-action" type="button" data-mastery ${stages.mastery ? '' : 'disabled aria-disabled="true"'}>${passed ? '再次挑战 Mastery →' : '开始 Mastery →'}</button>${passed ? '<button class="course-action primary inline-action" type="button" data-transfer>迁移挑战 →</button>' : ''}</div></section>
    ${!progress ? `<div class="course-completion-bar"><button class="course-action ${passed ? 'primary' : 'quiet'}" type="button" data-complete ${stages.complete ? '' : 'disabled aria-disabled="true"'}><span class="action-icon">✓</span><span><strong>完成本课</strong><small>${stages.complete ? 'Mastery ≥95%，现在可以完成本课' : `当前流程：先学习 → 实验 → 练习 → 诊断/补救 → ${masteryThreshold}% Mastery`}</small></span></button><div class="completion-hint">${stages.complete ? '已满足完成条件：Mastery 达标。' : `当前阶段：${phaseLabel(phase)}`}</div></div>` : ''}
  </section>`;
  root.querySelector('[data-back]')?.addEventListener('click', () => onBack?.()); root.querySelector('[data-quiz]')?.addEventListener('click', () => onStartQuiz?.()); root.querySelector('[data-mastery]')?.addEventListener('click', () => onStartMastery?.()); root.querySelector('[data-experiment]')?.addEventListener('click', () => onStartExperiment?.(experiments[0]?.id || experiments[0])); root.querySelector('[data-complete]')?.addEventListener('click', () => { if (passed) onComplete?.(); }); root.querySelector('[data-remediation]')?.addEventListener('click', () => onStartRemediation?.()); root.querySelector('[data-transfer]')?.addEventListener('click', () => onStartTransfer?.());
  root.querySelectorAll('.guided-submit').forEach(button => button.addEventListener('click', () => { const check = button.closest('.guided-check'); const selected = check?.querySelector('input[type="radio"]:checked'); if (!selected) return; onGuidedCheck?.(lessonId, check?.dataset.stepId, { correct: Number(selected.value) === Number.parseInt(check?.dataset.answer || '', 10), stepCount: steps.length }); }));
  if (highlightStep) { const card = root.querySelector(`.guided-learning-card[data-step-id="${CSS.escape(highlightStep)}"]`); if (card) { const detail = card.querySelector('.guided-card-detail'); if (detail) { detail.hidden = false; card.querySelector('.guided-card-header')?.setAttribute('aria-expanded', 'true'); const toggle = card.querySelector('.guided-card-toggle'); if (toggle) toggle.textContent = '−'; } requestAnimationFrame(() => card.scrollIntoView({ behavior: 'smooth', block: 'center' })); } }
}
const KNOWLEDGE_LABELS={'matter-change':'物质的变化','physical-change':'物理变化','chemical-change':'化学变化','physical-property':'物理性质','chemical-property':'化学性质','observation-inference':'观察与推理','evidence-reasoning':'证据与结论'};
function formatKnowledgePoint(id){return KNOWLEDGE_LABELS[id]||id;}
function renderGuidedCard(step,index,lessonId,guidedSteps={}){const body=Array.isArray(step.body)?step.body:[step.body];const check=step.check;const hasDetail=body.some(Boolean)||Boolean(check);const label=step.type==='concept'?'核心概念':step.type==='reasoning'?'判断方法':step.type==='transfer'?'迁移检查':step.type==='compare'?'综合辨析':'引导学习';const done=Boolean(guidedSteps[step.id]?.correct);const submitted=Number(guidedSteps[step.id]?.attempts||0)>0;return `<article class="guided-learning-card ${hasDetail?'':'compact'} ${done?'step-done':''}" data-step-id="${escapeHtml(step.id||'')}"><button class="guided-card-header" type="button" aria-expanded="true"><span class="guided-card-index">${String(index+1).padStart(2,'0')}</span><span class="guided-card-main"><span class="guided-card-label">第 ${index+1} 步 · ${label}${done?' <span class="guided-done-chip" style="color:var(--spec-green,#2e9e6b);font-weight:600">✓</span>':''}</span><strong>${escapeHtml(step.title||'')}</strong><small>${done?'✓ 已完成':submitted?'已提交 · 内容已展开':'内容已展开，直接学习'}</small></span><span class="guided-card-toggle" aria-hidden="true">−</span></button><div class="guided-card-detail"><div class="guided-step-body">${body.filter(Boolean).map(text=>`<p>${escapeHtml(text)}</p>`).join('')}</div>${check?renderCheck(check,index,step.id,lessonId,guidedSteps):''}</div></article>`;}
function renderLessonSections(sections){const list=Array.isArray(sections)?sections:[];if(!list.length)return '';return `<section class="course-section-block lesson-sections-block"><div class="course-section-heading"><div><span class="section-eyebrow">课程内容导航</span><h2>本课内容速览</h2><p class="section-description">各阶段要学习什么，先了解整体再开始。</p></div><span class="section-count">${list.length} 节</span></div><div class="lesson-sections-list">${list.map((section,index)=>`<article class="lesson-section-item"><span class="lesson-section-index">${String(index+1).padStart(2,'0')}</span><div><strong>${escapeHtml(section.title||'')}</strong>${Array.isArray(section.body)?section.body.filter(Boolean).map(text=>`<p>${escapeHtml(text)}</p>`).join(''):''}</div></article>`).join('')}</div></section>`;}
function renderCheck(check,index,stepId,lessonId,guidedSteps={}){const options=Array.isArray(check.options)?check.options:[];const stepState=guidedSteps[stepId]||{};const submitted=Number(stepState.attempts||0)>0;const correct=Boolean(stepState.correct);const resultClass=submitted?(correct?'correct':'retry'):'';const explanation=escapeHtml(check.explanation||'请回到本步骤的讲解，找出支持答案的证据。');return `<div class="guided-check" data-step-id="${escapeHtml(stepId||'') }" data-lesson-id="${escapeHtml(lessonId||'') }" data-answer="${Number.isInteger(check.answer)?check.answer:''}" data-explanation="${explanation}"><div class="guided-check-title">马上检查一下</div><p class="guided-question">${escapeHtml(check.question||'')}</p>${options.length?`<div class="guided-options">${options.map((option,i)=>`<label><input type="radio" name="guided-check-${index}" value="${i}"${submitted?' disabled':''}><span>${String.fromCharCode(65+i)}. ${escapeHtml(option)}</span></label>`).join('')}</div>`:''}<button type="button" class="guided-submit"${submitted?' disabled aria-disabled="true"':''}>${submitted?'已提交答案':'提交答案'}</button><div class="guided-result${resultClass?' '+resultClass:''}"${submitted?'':' hidden'}>${submitted?(correct?`✓ 回答正确。${explanation}`:`再想一想。${explanation}`):''}</div></div>`;}
function phaseLabel(phase){return {LEARNING:'学习中',EXPERIMENT:'实验待完成',PRACTICE:'练习待完成',DIAGNOSIS:'等待诊断',REMEDIATION:'需要补救',RECHECK:'等待复查',MASTERY:'等待掌握测试',MASTERED:'已掌握',COMPLETED:'已完成'}[phase]||'学习中';}
function escapeHtml(value){return String(value).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
if(typeof document!=='undefined')document.addEventListener('click',event=>{const link=event.target.closest('[data-guided-step]');if(link){const stepId=link.dataset.guidedStep;const card=document.querySelector(`.guided-learning-card[data-step-id="${CSS.escape(stepId)}"]`);if(card){const detail=card.querySelector('.guided-card-detail');if(detail){detail.hidden=false;card.querySelector('.guided-card-header')?.setAttribute('aria-expanded','true');const toggle=card.querySelector('.guided-card-toggle');if(toggle)toggle.textContent='−';}card.scrollIntoView({behavior:'smooth',block:'center'});}return;}const header=event.target.closest('.guided-card-header');if(header){const card=header.closest('.guided-learning-card');const detail=card?.querySelector('.guided-card-detail');if(!detail)return;const open=!detail.hidden;detail.hidden=open;header.setAttribute('aria-expanded',String(!open));const toggle=header.querySelector('.guided-card-toggle');if(toggle)toggle.textContent=open?'＋':'−';return;}const submit=event.target.closest('.guided-submit');if(submit){const check=submit.closest('.guided-check');const selected=check?.querySelector('input[type="radio"]:checked');const result=check?.querySelector('.guided-result');if(!selected||!result)return;const expected=Number.parseInt(check.dataset.answer,10);const correct=Number.parseInt(selected.value,10)===expected;result.hidden=false;result.textContent=correct?`✓ 回答正确。${check.dataset.explanation}`:`再想一想。${check.dataset.explanation}`;result.className=`guided-result ${correct?'correct':'retry'}`;
  // Disable resubmission: lock the check after first attempt.
  const btn=check?.querySelector('.guided-submit');if(btn){btn.disabled=true;btn.style.opacity='0.5';btn.style.pointerEvents='none';}}});
